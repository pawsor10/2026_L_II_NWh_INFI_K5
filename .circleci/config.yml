version: 2.1

jobs:
  build-and-test:
    machine:
      image: ubuntu-2004:current

    steps:
      - checkout
      - run: make depss
      - run: make testt
workflows:
  basic:
    jobs:
      - build-and-test









